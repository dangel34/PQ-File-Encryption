# pqfile Roadmap

This document tracks planned improvements, new features, and security work across future releases. Breaking changes to the `.pqf` format or public API always require a major version bump.

---

## v4.0.0 through the current release cycle (complete)

All features from v2.x onward (through 4.3.2, plus unreleased work already committed) are complete. Full detail, rationale, and scope decisions for every item below are in `docs/CHANGELOG.md`.

**Cryptographic core**: ML-KEM (512/768/1024) and hybrid X25519+ML-KEM-768 (FIPS 203); ML-DSA-65 signatures and signcrypt (FIPS 204); SLH-DSA-SHAKE-192f signatures (FIPS 205); optional formally-verified `libcrux-ml-kem` backend; key commitment in chunk-0 AAD, extended to full authenticated headers (`VERSION_AUTH_BIT`); memory-locked (`mlock`) secret storage; constant-time Shamir GF(256) arithmetic (branchless `gf_mul`/`gf_inv`); dudect-style constant-time test harnesses (Shamir, decrypt tamper paths, passphrase mismatch); algorithm-distrust mechanism for future deprecations.

**Wire formats**: v2-v11 covering streaming AEAD, multi-recipient (v4/v7 named, v8 anonymous, v9 padded-anonymous), passphrase-only (v10, with keyfile/FIDO2/WebAuthn-PRF second factors), stealth (magic-free), Padmé length padding, time-locked (v11, drand-based), archives (PQFA) - plus compat-vector golden fixtures for every wire behavior and a cross-version compatibility matrix (v2-v9+).

**Key management**: Shamir M-of-N threshold splitting/reconstruction, key revocation, hardware-backed keys via OS credential store, signable certificates with revocation lists, rekey/add-recipient (zero-copy), recursive/symlink-safe archiving, compact `pqf1…` Bech32m recipient strings, `pqfile fingerprint`/`--qr`.

**APIs**: streaming `PqfWriter`/`PqfReader`, async I/O (Tokio), pipelined/mmap/multithreaded-compress encryption, `MultiEncryptBuilder`, progress callbacks, `SeekableDecryptor`, resumable/checkpointed encryption, structured JSON error codes (`docs/ERROR_CODES.md`), `inspect_stream`, `#![deny(unsafe_code)]` at the crate root, `STABILITY.md` promise.

**Tooling**: CLI (`pqfile`) and native/WASM GUI sharing one core library, `doctor` diagnostics with Argon2id auto-calibration, `check`/`check --parallel`, sealed sender, steganographic key backup, encrypted audit logging, forward-error-correction sidecars, default recipient config file, `--force` overwrite protection, interactive no-args mode, shell completions, man pages, `--threads` cap.

**Supply chain / CI**: published to crates.io, `cargo vet`/`cargo deny`/`cargo semver-checks`/`zizmor` gates, SLSA provenance + `cargo auditable` + PGO release binaries, scheduled advisory scanning, CodeQL, deterministic (`iai-callgrind`) and wall-clock benchmark gates, property-based (`proptest`) and mutation (`cargo-mutants`) testing, NIST/Wycheproof known-answer test vectors, cross-platform native installer packaging (unsigned), GUI accessibility (AccessKit) coverage.

See `docs/CHANGELOG.md` for the full chronological detail behind every item above, including scope corrections and design rationale for each.

---



## v4.x - Planned (no breaking format changes)

All ranked items from the 2026-07-08 ecosystem review (CodeQL, the iai-callgrind gate, the libcrux-ml-kem backend, and PGO) are done and moved to the completed list above, along with 2-5 of an even earlier ranking (cargo-semver-checks, advisory scan, zizmor, compat vectors) and the memory-locking item. Both certificate follow-ups flagged when the cert module shipped on 2026-07-13 - CLI `sign`/`verify`/`signcrypt`/`signdecrypt` support and GUI support - are also done as of the same day (moved to the completed list above). v4.3.0 shipped 2026-07-11; the project is staying on 4.x for now, so the v5.0 section stays parked until there's a reason to cut a major - see that section for the ranked backlog once it reopens.

Six items were newly scoped by a 2026-07-21 codebase review, ranked by value versus implementation cost (cheapest, highest-confidence wins first). All six are now done (moved to the completed list above).

### Standing guideline: BLAKE3 for new non-format hashing

SHA3-256 stays everywhere it appears in the wire format or key fingerprints (compatibility is binding). But new surfaces that never touch the format, such as a future audit log, `doctor` file hashing, or dedup checks, should prefer `[blake3](https://crates.io/crates/blake3)`: roughly 10x faster and internally parallel. Recorded here so SHA3 does not get baked into new features by reflex.

---



## v5.0 - Next major (breaking format changes)

These items require a new major version because they change the wire format or public API in a backward-incompatible way (or, for new KEM/cipher variants, produce files that older readers reject). Ranked.

- **X-Wing as the hybrid KEM (standards alignment)**
Replace the bespoke X25519+ML-KEM-768 combiner with [X-Wing](https://datatracker.ietf.org/doc/draft-connolly-cfrg-xwing-kem/), the IETF CFRG general-purpose PQ/T hybrid KEM, using the RustCrypto `[x-wing](https://github.com/RustCrypto/KEMs/tree/master/x-wing)` crate (built on the same `ml-kem` and `x25519-dalek` pqfile already depends on). X-Wing carries a formal security proof (secure if SHA-3 and either component is secure) and is on an RFC track, which buys interoperability and external review that a homegrown combiner never gets. Technically this could ship additively as a new KEM variant ID in 4.x, but since older readers cannot read the files it belongs with the other compatibility-affecting work. The existing hybrid variant stays readable forever. Code anchors: the current bespoke combiner is `hybrid_hkdf` in `pqfile/src/format.rs` (HKDF-SHA256 over both shared secrets), hybrid keygen lives in `pqfile/src/keygen.rs`, and KEM variant IDs are defined in `format.rs`; X-Wing replaces the combiner and the two-ciphertext slot layout with the crate's single opaque KEM, so the recipient-slot writing and parsing code changes too. Status as of 2026-07-13: still an individual Internet-Draft (`draft-connolly-cfrg-xwing-kem-10`), not yet adopted by an IETF working group or issued as an RFC; the "to be removed before publishing as an RFC" note is still present in the draft. Worth tracking but not yet blocking - the RustCrypto crate already tracks the draft closely enough to prototype against.
- **Per-file entry AEAD in archives (PQFA v2)**
The current `.pqfa` format authenticates the entire archive before any file is extracted, which requires buffering the full ciphertext in memory for in-memory extractions. A PQFA v2 layout gives each file entry its own AEAD tag derived from the session key and the entry index, so individual files can be extracted and verified without loading the whole archive. Design notes for when this happens: bind both the entry index and the entry name into each entry's AAD (index prevents undetected reordering, name prevents undetected renames), authenticate the manifest on its own before streaming any entry, and keep the per-entry `mtime_secs`/`mode` restore working from the v1 work. The archive code is isolated in `pqfile/src/archive.rs`.
- `PqfileError` **refinement** *(substantially complete)*
`Truncated`, `UnsupportedVersion`, and `NoMatchingRecipient { slots_tried }` already exist as distinct variants, and `DecryptionFailure` is now returned only for genuine AEAD tag mismatches (plus deliberate anti-oracle collapsing of malformed-ciphertext cases). The only remaining piece of the original item is renaming `DecryptionFailure` → `AuthenticationFailure`, a pure rename w
- 
- **Misuse-resistant nonces (nonce-SIV construction)**
Replace random nonces with synthetic nonces derived from a hash of the session key and plaintext chunk (a simplified SIV mode). With random nonces, a nonce collision is possible if the same key encrypts a very large number of chunks; SIV derivation makes collision probability zero regardless of how many files are encrypted under a given session key. This is a format break because the nonce field changes meaning in the chunk header. Lower priority than it may sound: every file already gets a fresh session key, so the collision scenario this defends against is already negligible in practice.
- **AEGIS as an optional AEAD suite**
[AEGIS-128L / AEGIS-128X / AEGIS-256](https://github.com/jedisct1/rust-aegis) (CFRG draft `draft-irtf-cfrg-aegis-aead`) is dramatically faster than both AES-GCM and ChaCha20-Poly1305 on CPUs with AES acceleration, natively key-committing (pqfile currently bolts commitment on via the chunk-0 AAD), and has a large nonce space. A new cipher ID in the header means older readers reject the files, hence v5.0. Caveats that keep it below X-Wing: the performant path of the `aegis` crate links C (`libaegis`) via `cc` while the pure-Rust fallback is much slower, the WASM story is weak, and ChaCha20-Poly1305 already saturates most storage I/O, so the practical win is narrower than the benchmark gap suggests.

---



## New Directions

Ideas not yet implemented, focused on cryptographic depth or ecosystem reach. Ordered roughly by value multiplied by implementation readiness as of the 2026-07-08 review, with four new entries added by the 2026-07-13 review (marked below) inserted at their approximate rank, plus one addition to the existing language-bindings item (also marked); entries near the top have mature building blocks available today, entries near the bottom are research-grade or blocked on external events. Five more entries (20-24) were added by a 2026-07-14 competitor/ecosystem review, appended at the end rather than re-ranked into the list above. A 2026-07-20 status-check review updated items 3 and 5 in place and appended item 25. Seven more entries (26-32) were added by a 2026-07-23 roadmap-expansion review, appended at the end rather than re-ranked into the list above.

### 1. Time-locked encryption *(shipped 2026-07-15, pure time-lock only)*

Shipped as the v11 format over `tlock`/`drand_core` against the League of Entropy `quicknet` chain, behind an off-by-default `tlock` Cargo feature (`encrypt --tlock-round`/`decrypt --tlock`/`pqfile tlock round`). Pure time-lock only - no recipient key pair, anyone with the ciphertext can decrypt once the round fires - and not post-quantum (BLS12-381 pairing-based), documented at every user-facing surface. Possible follow-up, not yet scoped: combining a tlock-derived secret with a real KEM recipient's shared secret (an AND-gate needing both the round *and* a specific private key) is a materially different design from what shipped. Full detail (testing approach, compat-vector rationale) in `docs/CHANGELOG.md` and `docs/FORMAT.md` §5.12.

### 2. age ecosystem interoperability

age v1.3.0 ships native post-quantum hybrid ML-KEM-768+X25519 recipients, and the [age plugin protocol](https://words.filippo.io/age-plugins/) makes third-party recipient types first-class in every age client (including rage, the Rust implementation). An `age-plugin-pqfile` binary exposing pqfile identities as age recipients would put pqfile keys inside the largest modern file-encryption ecosystem without users switching tools. Independently worth stealing: age's `RecipientWithLabels` anti-downgrade mechanism, where a recipient labeled `postquantum` refuses to be mixed with weaker recipients in one file; pqfile's multi-recipient modes currently have no equivalent policy check when mixing, say, an ML-KEM-1024 slot with an ML-KEM-512 slot.

### 3. OpenPGP-PQC interoperability *(added 2026-07-13)*

`[draft-ietf-openpgp-pqc](https://datatracker.ietf.org/doc/draft-ietf-openpgp-pqc/)` (extending RFC 9580) defines composite ML-KEM+X25519 encryption and composite ML-DSA+Ed25519 signatures for OpenPGP, alongside standalone SLH-DSA; it reached draft -17 on 2026-01-13 and is expected to publish in H1 2026, with Sequoia PGP committed to shipping support shortly after. This is a much larger existing user base than age's plugin ecosystem (item 2), and GnuPG/Sequoia users are exactly the population most likely to already care about post-quantum key material. Shape is different from the age plugin approach, though: there's no third-party-recipient extension point, so this would mean either exporting/importing pqfile key material as OpenPGP-PQC-compatible packets (a real ASN.1/OpenPGP-packet-format effort, not a thin adapter) or, more realistically, a standalone `pqfile pgp-export`/`pgp-import` conversion utility scoped to the composite-key case only. Track the draft to final publication before scoping further; revisit alongside the FN-DSA item below since both are pinned on a NIST/IETF document finalizing in a similar window.

**Status update (2026-07-20)**: the draft has moved to **AUTH48** (final copy-edit stage before publication) with an RFC number already assigned - **RFC 9980**. This is the exact publication trigger the note above was waiting on; the item should move up in priority at the next planning pass. The scoping problem is otherwise unchanged: no third-party-recipient extension point, so this stays an export/import conversion utility rather than a thin adapter.

**Status update (2026-07-21): RFC 9980 published (June 2026, Standards Track) - scoped, and deferred.** A detailed pass against pqfile's actual key types found the real overlap is much narrower than the shape assumed above:

- **Hybrid encryption keys (**`X25519+ML-KEM-768`**) are the one clean match.** pqfile already stores these as a raw X25519 public key appended to a raw ML-KEM-768 encapsulation key (private side: a 32-byte X25519 scalar plus a 64-byte ML-KEM seed - see `pqfile/src/keygen.rs`). RFC 9980's algorithm ID 35 is exactly ML-KEM-768+X25519, and it's the RFC's one *mandatory-to-implement* composite. A key-material export/import is genuinely reachable here.
- **Plain ML-KEM-512/768/1024 (non-hybrid) keys have no OpenPGP-PQC equivalent at all.** RFC 9980 defines no standalone ML-KEM option - composite-with-ECDH is the only encryption path in the whole spec.
- **ML-DSA-65 signing keys are not a straight export.** RFC 9980 mandates ML-DSA-65 only paired with a fresh Ed25519 key (composite signature, both components must verify), so an existing pqfile ML-DSA-65 key can't become an OpenPGP key on its own - it would need a newly-minted companion Ed25519 key, which is really "issue a new OpenPGP identity that happens to reuse your ML-DSA key," not a conversion of the existing one.
- **SLH-DSA-SHAKE-192f signing keys have no mapping, full stop.** RFC 9980 specifies exactly three standalone SLH-DSA options - SHAKE-128s, SHAKE-128f, and SHAKE-256s (algorithm IDs 32-34) - and no 192-bit security level at all. pqfile's own SLH-DSA parameter set simply isn't representable in OpenPGP-PQC as it stands.

So a conversion utility's realistic scope collapsed to *hybrid encryption keys only* - narrower than "the composite-key case" phrasing above implied, since that reads as covering both KEM and signature composites. Separately, the obvious implementation dependency, `sequoia-openpgp`, only has PQC support in a pre-release fork (`2.2.0-pqc.1`, needing `--features crypto-openssl`, a system OpenSSL dependency) with no stable-release date even now that the RFC is published - conflicts with this project's consistent rustls-only stance (`tlock`/`update-check` both deliberately avoided system OpenSSL via `ureq`+rustls).

**Decision: parked, not building the narrow version.** Given signing-key interop - arguably the more valuable half, since GnuPG/Sequoia users care at least as much about verifying mail signatures as encrypting to a recipient - isn't reachable at all today, and the one clean win (hybrid encryption keys) would still mean depending on an unstable OpenSSL-requiring pre-release crate, this isn't worth building yet. Revisit when either: (a) `sequoia-openpgp` ships a stable, non-`crypto-openssl`-only PQC release, or (b) a future OpenPGP-PQC revision adds standalone ML-KEM or a 192-bit SLH-DSA parameter set, meaningfully widening what a conversion utility could actually cover.

### 4. FN-DSA (Falcon) signatures

FIPS 206 went to draft approval in August 2025 with the final standard expected late 2026 or early 2027. FN-DSA signatures are ~666 bytes versus ~3.3 KB for ML-DSA-65, a major win for signcrypt overhead and QR-code-sized artifacts. Thomas Pornin's `[rust-fn-dsa](https://github.com/pornin/rust-fn-dsa)` is high quality and tracks the draft. The PEM-tag-based algorithm auto-detection added for SLH-DSA means a third signature algorithm slots into every sign/verify/signcrypt path the same way. Blocked on: the standard finalizing, and the crate stabilizing against the final test vectors. Revisit each quarter. Status as of 2026-07-13: unchanged from the prior review - NIST's IPD is still open for public review, no published FIPS 206 yet.

### 5. Threshold ML-DSA signing *(added 2026-07-13)*

Extend pqfile's existing M-of-N story (Shamir threshold **decryption**) to threshold **signing**: split a CA or long-term signing key across N custodians such that any K can jointly produce a valid, standard-verifying ML-DSA signature without ever reconstructing the private key on one machine. The direct motivating use case is the new `pqfile issue-cert --ca-key`: today the CA private key is a single file an operator must protect entirely on their own, which is a weak link for anything acting as a real certificate authority. This is genuinely research-grade right now, not a "pick a crate" task: 2026 saw several competing constructions (Mithril, Hermine, Quorus, and "Masked Threshold ML-DSA" via Shamir nonce DKG) but none has a mature, audited Rust implementation, and per the NIST CSRC threshold-signatures track most existing threshold schemes are still incompatible with the standardized FIPS 204 verifier - "Masked Threshold ML-DSA" is notable specifically because it claims standard-verifier compatibility. Revisit in 6-12 months once one of these constructions has a real crate; premature to scope further today.

**Status update (2026-07-20)**: a real crate now exists - `[threshold-ml-dsa](https://crates.io/crates/threshold-ml-dsa)` (lattice-safe org), a paper-faithful implementation of the Mithril construction (ePrint 2026/013) for threshold ML-DSA-44: FIPS 204 bit-for-bit-verifier-compatible output, `no_std`-compatible, zeroize-on-drop, K-parallel repetitions with hyperball rejection sampling, scaling from 2-of-3 to 32-of-45 thresholds at sub-100ms latency. This crosses the "no mature Rust implementation" blocker named above, so the item is now a concrete "prototype against this crate" task rather than a wait-for-the-field one - but it is not independently audited, so that should be a prerequisite (or at least an explicit accepted-risk call) before it backs a real CA signing key, not a reason to defer further research.

**Status update (2026-07-21): re-checked against the crate's actual README/docs.rs (v0.3.6) - the 2026-07-20 note above overstated readiness, correcting here.** The real supported range is **15 (T,N) configurations from (2,2) to (6,6) - max 6 total parties**, not "2-of-3 to 32-of-45"; that figure appears to have been a misread, possibly of the underlying paper's theoretical range rather than what the shipped SDK exposes. Beyond the scale correction, several harder problems surfaced against pqfile's actual use case:

- **Fresh keygen only, not key splitting.** `ThresholdMlDsa44Sdk::from_seed` generates independent threshold secrets from scratch; it does not decompose an *existing* ML-DSA private key into shares. The motivating problem above - "today's `issue-cert --ca-key` is a single file" - isn't solvable for a CA that already exists this way; it only helps someone setting up a brand-new CA from day one.
- **ML-DSA-44 only, not 65.** pqfile's `cert` module requires ML-DSA-65 (Category 3) or SLH-DSA-SHAKE-192f for CA keys today. This crate is Category 2 only, so adopting it means either a security-category downgrade for threshold CAs specifically, or a new, lower-security CA key type alongside the existing ones.
- **Explicitly not constant-time.** The README states hyperball sampling uses floating-point (`f64`/`libm`) and "is not strictly constant-time," recommending deployments "isolate party execution" - a real regression against this project's own constant-time bar (three dudect harnesses elsewhere), and not something a CLI can practically guarantee for the caller.
- **A second, independent ML-DSA implementation.** Depends on `dilithium-rs`, not the RustCrypto `ml-dsa` pqfile already uses everywhere else - two unaudited ML-DSA implementations in the tree instead of one.
- **Real multi-machine deployment needs the caller to build the transport.** The 3-round commit/reveal/respond protocol is only fully wired for single-process demos; genuine separate-custodian deployment means designing and implementing authenticated networking between parties, outside the crate's scope.

**Decision: parked, not prototyping against this crate now.** The combination of small max N, Category 2-only, fresh-keygen-only (doesn't protect an existing CA key, the actual motivating problem), unaudited, and explicitly non-constant-time is closer to "the field now has one candidate crate" than "ready to build a real feature on." Revisit once N scales further, ML-DSA-65 is supported, key-splitting (not just fresh keygen) is possible, or an independent audit lands.

### 6. Sealed sender *(shipped 2026-07-15)*

Shipped as `pqfile::sealed_sender` (CLI `identity-keygen`/`seal`/`unseal`, GUI "Sealed Sender" tab). No wire-format change - a sealed-sender file is a standard v3 `.pqf` file whose plaintext payload is `tag ++ original plaintext` (`docs/FORMAT.md` §9). Uses a static-static X25519 Diffie-Hellman between a new, separate long-term identity key pair per party (rather than an ephemeral signing key) so deniability holds without ever producing a signature object; intentionally separate from both the ML-KEM encryption key and any ML-DSA/SLH-DSA signing key, so it never affects `signcrypt`'s non-repudiation properties. Full detail in `docs/CHANGELOG.md`.

### 7. Python, Node.js, and mobile bindings *(Python and Node.js shipped 2026-07-24; mobile binding layer shipped same day, Android/iOS packaging still open)*

Expose core `pqfile::encrypt` and `pqfile::decrypt` as a Python wheel and an npm package. The modern pairing is PyO3 with `[maturin](https://github.com/PyO3/maturin)` for wheel builds across the manylinux/macOS/Windows matrix, and `[napi-rs](https://napi.rs)` for prebuilt native Node addons (faster than the WASM path for server-side use; the existing wasm-bindgen build remains the browser fallback). Allows Python and Node.js scripts to encrypt and decrypt without shelling out to the CLI. *(2026-07-13 addition:)* the same core is currently unreachable from a native mobile app - there's a desktop GUI and a WASM web app, but no iOS or Android story. `[uniffi-rs](https://github.com/mozilla/uniffi-rs)` (Mozilla, battle-tested in Firefox for Android/iOS) generates Kotlin and Swift bindings from one Rust interface definition and would sit naturally alongside the PyO3/napi-rs work as a third binding target off the same core crate. Packaging is the long pole here (Swift Package Manager / XCFramework distribution, Maven/Gradle publishing, no App Store review needed since there's no app - just a library), which is why it's grouped with the other bindings work rather than scoped standalone. *(2026-07-14 addition:)* the mobile case specifically has sharpened into a real market gap, not just a completeness gap: both Apple (CryptoKit ships X-Wing) and Android (Keystore added ML-DSA in Android 17) now expose PQC natively at the OS level, yet no consumer post-quantum file-encryption app ships on either app store today. A minimal pqfile mobile shell over the `uniffi-rs` bindings would be a genuine first-mover, not just feature parity.

*(2026-07-24: Python and Node.js fully done; mobile's binding layer is done but Android/iOS packaging is not.)* Shipped as `pqfile-python/`: a PyO3 extension module (`_pqfile`, built via maturin, mixed python/rust layout) wrapping `keygen`/`keygen_hybrid`/`encrypt_bytes`/`decrypt_bytes`/`encrypt_file`/`decrypt_file` - the single-recipient streaming path only (multi-recipient, signing/`signcrypt`, Shamir, and certificates are not yet exposed). Deliberately excluded from the root Cargo workspace (`exclude` list, same treatment as `fuzz/`) so `cargo build --workspace` never needs Python dev headers; maturin drives its build independently with its own `Cargo.lock`. Required `pyo3 = "0.29"` rather than the initially-tried `"0.24"`, which caps out at Python 3.13 (this machine runs 3.14). Errors surface as `pqfile.PqfileError` with the stable numeric code from `PqfileError::code()` (`docs/ERROR_CODES.md`) appended to the message. 9 pytest round-trip tests pass (all three ML-KEM levels, hybrid, passphrase-protected keys including wrong-passphrase/missing-passphrase failure, empty plaintext, wrong-key rejection, file-to-file streaming, invalid PEM rejection). *(2026-07-24, later the same day: CI added)* - `ci.yml`'s `bindings-python` job builds via maturin and runs the pytest suite on every push/PR. *(2026-07-24, later still: publish scaffolding added, not yet functional)* - `publish-python.yml` builds manylinux/Windows/macOS (x86_64 + aarch64) wheels plus an sdist via `PyO3/maturin-action` and uploads to PyPI on a GitHub Release being published, mirroring `publish.yml`'s crates.io convention (`environment: release` gate). Publishes via PyPI Trusted Publishing (OIDC, `pypa/gh-action-pypi-publish` + `id-token: write`) rather than a stored `PYPI_API_TOKEN` - PyPI's classic API-token creation flow turned out to be gone entirely for a project that doesn't exist yet; only the "pending publisher" OIDC registration flow is offered, which settled the token-vs-OIDC choice the README had left open. Has never actually run - the pending publisher (project `pqfile`, owner `dangel34`, repo `PQ-File-Encryption`, workflow `publish-python.yml`, environment `release`) is not yet registered on PyPI. "pqfile" is confirmed available as a package name on PyPI (checked 2026-07-24).

Same day, shipped `pqfile-node/`: a napi-rs native addon exposing the identical function set (camelCase: `keygen`/`keygenHybrid`/`encryptBytes`/`decryptBytes`/`encryptFile`/`decryptFile`). Every function is a napi-rs `Task` run via `AsyncTask` on libuv's worker thread pool, resolving a JS `Promise`, rather than running synchronously on Node's single-threaded event loop - Argon2id and ML-KEM are CPU-heavy enough that inline execution would stall whatever else the process is doing (a real correctness concern for a server, unlike Python's GIL release which only affects other Python threads). `#[napi(ts_return_type = "...")]` annotations were needed on every `AsyncTask`-returning function; without them napi-rs's typegen emits `Promise<unknown>` instead of the real resolved type, since it can't infer `Task::JsValue` through the wrapper automatically. Also excluded from the root Cargo workspace so `cargo build --workspace` never needs Node headers; `napi build` drives it independently with its own `Cargo.lock`. Errors surface as a plain JS `Error` with the same `(code N)` suffix convention as the Python binding. 9 tests pass under Node's built-in test runner (`node --test`), mirroring the Python suite's coverage. *(2026-07-24, later the same day: CI added)* - `ci.yml`'s `bindings-node` job runs `npm install && npm run build && npm test` on every push/PR. *(2026-07-24, later still: publish scaffolding added, not yet functional)* - `publish-node.yml` cross-builds the addon for Windows/Linux x64 and macOS x86_64/aarch64, arranges them into napi-rs's standard per-platform `optionalDependencies` packages, and publishes on a GitHub Release being published. Writing this exposed a real bug: `pqfile-node/package.json`'s `napi` config used a `binaryName`/`targets` schema napi-rs doesn't actually read (the real keys are `napi.name` and `napi.triples.{defaults,additional}`), so `napi create-npm-dir` had been silently creating only 3 of the 4 intended per-platform directories since the crate was first scaffolded - fixed and re-verified locally (all 4 directories generate correctly now; `napi artifacts` was confirmed to correctly pick up a fake downloaded-artifact directory). `aarch64-unknown-linux-gnu` stays out of the target list for now - cross-compiling it needs a zig toolchain step not yet wired up. Publishes via npm Trusted Publishing (OIDC, `id-token: write`, no stored token) rather than a classic "Automation" token - npm's own token-creation UI warns against the 2FA-bypass automation token this would otherwise need for unattended CI and points at Trusted Publishing instead, settling that choice the same way PyPI's missing classic-token option settled it for Python. Unlike PyPI's pending-publisher flow, npm's Trusted Publisher can only be registered from an *existing* package's settings page, so the very first publish of "pqfile" will need a one-off manual `napi prepublish` run from a maintainer's own authenticated npm login before Trusted Publishing can take over. Never actually run end-to-end - the npm Trusted Publisher isn't registered yet, and the non-Windows build legs were never exercised on this repo's Windows dev machine. "pqfile" is confirmed available as a package name on npm (checked 2026-07-24).

Same day, `pqfile-mobile/` also got CI coverage without needing an Android/iOS toolchain: `ci.yml`'s `bindings-mobile` job runs the crate's 7 Rust-level tests, then re-runs `uniffi-bindgen generate` for both Kotlin and Swift as a sanity check that the exported interface still produces valid binding source - not a substitute for actually compiling or running that source (which stays impossible without the NDK/Xcode), but real coverage against the one thing that actually could regress silently here: a Rust-side signature change breaking what uniffi generates. All three bindings crates (`bindings-python`/`bindings-node`/`bindings-mobile` jobs) run on `ubuntu-latest` only, matching every other job in `ci.yml` - cross-platform validation is release.yml's job, not the main CI suite's.

Same day, started (not finished) `pqfile-mobile/`: a uniffi-rs crate exposing the same function set via `#[uniffi::export]`-annotated plain Rust functions, generating both Kotlin and Swift bindings from this one definition. Unlike the Python/Node.js bindings these calls are synchronous - there's no single async runtime shared by Kotlin coroutines and Swift's `async`/`await` worth picking a side on, so callers dispatch off their own main thread themselves, same as any other blocking native call. **What's actually verified**: the crate builds for the host target, `cargo test` passes (7 cases calling the exported functions directly as Rust, mirroring the Python/Node suites - there's no way to drive the Kotlin/Swift FFI boundary itself without those toolchains), and `uniffi-bindgen generate --library <built lib> --language kotlin|swift` produces Kotlin and Swift source that reads correctly on inspection (right camelCase names, `throws`/checked-exception signatures, `ByteArray`/`Data` for buffers). **What's not done, and could not be done on this repo's Windows dev machine**: no Android NDK is installed, so no `aarch64-linux-android` cross-compile or Gradle module exists; no Xcode (which only runs on macOS) means no iOS cross-compile, XCFramework, or Swift package either; and the generated Kotlin/Swift source has never actually been compiled or executed by a Kotlin or Swift toolchain, only read over by eye. This is the packaging long pole flagged in this item's own framing since 2026-07-13 - closing it needs someone continuing on a Mac (for iOS) and/or a machine with the Android NDK installed, not further work possible here.

### 8. Shell integration

Right-click "Encrypt with pqfile" on Windows (Explorer context menu via registry entry), macOS (Quick Action via Automator bundle), and Linux (`.desktop` file). The integration invokes the CLI with the last-used recipient key and writes the output alongside the original.

### 9. Native OS installer packaging *(unsigned packages shipped 2026-07-16; signing still open)*

Shipped: `.deb` (`cargo-deb`) and `.rpm` (`cargo-generate-rpm`, pure Rust, no `rpmbuild`/`rpm-build` package needed) for Linux, a Linux AppImage (`linuxdeploy` + its appimage plugin, requires `squashfs-tools`) for the desktop GUI, and a macOS `.app` bundle + DMG (`create-dmg --no-code-sign`) for the desktop GUI - all built in `release.yml`'s existing `build` job from the already-built, PGO-optimized, `cargo auditable` binaries rather than rebuilding, and uploaded alongside the pre-existing Windows Inno Setup installer. New packaging assets: `pqfile-desktop/packaging/pqfile-desktop.desktop`, `pqfile-desktop/packaging/Info.plist.template`.

cargo-dist was evaluated per the original note below and turned out not to fit: it does not build DMG, `.deb`, `.rpm`, or AppImage at all (macOS `.dmg`/`.app` support is an open upstream issue, axodotdev/cargo-dist#24, marked out of scope since the MVP) - it's strong at MSI (already covered here via Inno Setup) plus shell/PowerShell `curl|sh` installers and a Homebrew tap, none of which close this item's actual gap. Point tools were used instead, each as a new conditional step in the existing per-target matrix job rather than a pipeline replacement, so the PGO/SLSA/cosign/SBOM machinery already in `release.yml` was untouched. A narrow cargo-dist adoption for the shell/PowerShell installers and Homebrew tap - genuinely new distribution channels this project doesn't have - is still worth scoping separately later; it was deliberately not bundled into this pass.

**Still open, by explicit scope decision**: none of the new packages (or the pre-existing Windows installer) are code-signed or notarized. Windows SmartScreen and macOS Gatekeeper will both warn on first launch; macOS users need to right-click → Open once to bypass Gatekeeper. Closing this needs a Windows code-signing certificate (~$100-400/yr from a CA, or a subscription service like Azure Trusted Signing/SignPath) and an Apple Developer Program membership ($99/yr) plus a Developer ID certificate and `notarytool` wired into CI with an API-key secret - both are cost/account decisions for the project owner, not implementation work, which is why they were scoped out of this pass rather than attempted partially.

### 10. Hardware Security Module (HSM) / PKCS#11 support *(added 2026-07-13)*

pqfile's current "hardware-backed key" story is the OS credential store (Windows DPAPI, macOS Keychain, Linux Secret Service) - convenient, but not a real HSM or smartcard boundary, and not what an organization with a compliance requirement for hardware key custody actually needs. PKCS#11 v3.2 now standardizes mechanisms for ML-KEM, ML-DSA, and SLH-DSA (without external-mu), and 2025-2026 saw several HSM vendors (Entrust nShield 5, Thales Luna G7/K7, Marvell LiquidSecurity 2, Securosys, Utimaco) obtain CAVP certification for the NIST PQC algorithms with FIPS 140-3 validation in progress. The `[cryptoki](https://docs.rs/cryptoki)` crate is a maintained Rust PKCS#11 binding. Ranked below the language-bindings and shell-integration items because deployed PQC-capable HSM hardware is still early - not every vendor ships it yet - so the addressable user base today is small; revisit as hardware availability broadens. Would slot in as a new key-source alongside the existing plaintext/passphrase-encrypted/OS-credential-store options, additive and non-breaking to the wire format.

### 11. Certificate revocation *(shipped 2026-07-15)*

Shipped as `pqfile::cert::revoke_cert`/`verify_revocation_list`/`check_cert_not_revoked`, CLI `pqfile revoke-cert`, and an optional `--revocations <FILE>` flag on every command that resolves a certificate. A revocation list is a CA-signed sequence of `(cert_id, revoked_at, reason)` entries - a compact analogue of an X.509 CRL, `cert_id` being SHA3-256 of the certificate's own signed body so no format change to `Certificate` was needed. File-based only, no OCSP-style online responder, by design. Full detail in `docs/CHANGELOG.md` and `docs/FORMAT.md` §6.5.

### 12. Encrypted audit log *(shipped 2026-07-22)*

Shipped as `pqfile::audit` (`audit` cargo feature) plus CLI `--audit-log`/`--audit-key`/`--audit-recipient` on `encrypt`/`decrypt` and `pqfile audit-verify --expect-tip <HEX>`. Each record is signed with the operator's own ML-DSA/SLH-DSA key, then encrypted to a separate auditor's ML-KEM public key, with a hash chain detecting silent deletion/reordering; `--expect-tip` additionally catches truncation off the end of the log. Scoped to encrypt/decrypt only; `pqfile-gui` support is native-only (the web build's download-only model can't back persistent append-only storage). Full detail in `docs/CHANGELOG.md`.

### 13. Split ciphertext storage

A mode where the raw ciphertext bytes are split across N output files using a secret sharing scheme (or simpler XOR splitting for K=N), requiring any K files to reconstruct. Different from key splitting: the key stays intact and the payload itself is distributed. Useful for backup scenarios where the ciphertext is spread across cloud providers that are mutually untrusted; no single provider has a usable ciphertext.

### 14. Key ceremony tooling

An interactive guided ceremony mode for high-assurance key generation. Multiple participants each contribute entropy combined via SHA3-256 before seeding key generation so no single participant can bias the result. The ceremony log records each participant's entropy hash, the combined seed hash, and the resulting public key fingerprint.

### 15. Attribute-based access control policies

Go beyond M-of-N threshold decryption to support Boolean access policies: "decrypt if holder of key A AND key B, OR key C." Each policy node is an encrypted share of the session key. Evaluation is a tree walk using Shamir recombination at AND nodes and branch selection at OR nodes.

### 16. Web extension / browser integration

A browser extension (Chrome / Firefox) that embeds the existing WASM core and adds an "Encrypt" action to file-attachment dialogs and an "Encrypt text" context menu item. Encryption runs entirely in the browser process; no data is sent to a server.

### 17. Deniable encryption

Produce a `.pqf` file that yields two valid, indistinguishable plaintexts: a real one under the primary key and a decoy under a duress key. Both decrypt without error and leave no detectable marker distinguishing which is real. VeraCrypt offers this for full-disk volumes but no post-quantum file encryptor provides it. The design challenge is two independently valid ML-KEM shared secrets each mapping to a distinct AEAD layer, with a header that reveals nothing about which layer is authoritative. *(2026-07-14 addition:)* VeraCrypt's specific mechanic is worth studying as an alternative shape to the single-file dual-layer design above: a "hidden volume" is written into the free-space slack of an outer volume that itself holds real (if less sensitive) content, so revealing only the outer passphrase under coercion is itself plausible rather than obviously partial. Combined with pqfile's existing Padmé length-padding, an outer `.pqf` padded well beyond its real content, with a hidden inner ciphertext occupying the padding slack, could reuse `padding.rs` machinery rather than requiring a wholly new dual-AEAD-layer format.

### 18. Forward-secret file exchange protocol

A stateful protocol built on pqfile that provides forward secrecy for an ongoing file exchange session between two parties. Each exchange ratchets a shared root secret forward using a new ML-KEM encapsulation, so compromise of the current session key does not expose previously exchanged files. State lives in a small JSON ratchet file alongside the key pair.

### 19. Proxy re-encryption

Generate a re-encryption key `rk(A -> B)` from private key A and public key B. A proxy holding only `rk` can transform a ciphertext encrypted for A into one encrypted for B, without ever seeing the plaintext or either private key. Useful for delegated access: a file server can re-encrypt stored files on behalf of a new recipient without the sender needing to re-encrypt manually. Ranked last among the cryptographic items because no practical post-quantum PRE construction with a mature implementation exists; the known lattice-based schemes are research-grade, and falling back to a classical ECC-based PRE would break the project's post-quantum story.

### 20. WebAuthn PRF for the WASM web app *(implemented 2026-07-15, disabled in the UI pending browser/OS support)*

Shipped as a new v10 header flag bit (`V10_FLAG_WEBAUTHN_PRF`) and a wasm32-only `pqfile-gui::webauthn` module: `navigator.credentials.create()`/`.get()` with the `prf` extension, reached via hand-written `#[wasm_bindgen(inline_js = "...")]` glue rather than web-sys's Credential Management bindings (those require an explicit `--cfg=web_sys_unstable_apis` opt-in across every build entry point - avoided entirely). New library API `encrypt_stream_passphrase_webauthn_prf[_with_params]` / `decrypt_stream_passphrase_webauthn_prf[_with_limits]`, new errors `WebauthnPrfRequired`/`WebauthnPrfNotRequired` (codes 33/34). See `docs/CHANGELOG.md` (Unreleased) and `docs/FORMAT.md` §5.9 for full details.

Scope notes worth recording: (1) registers a **resident** (discoverable) credential, matching the W3C PRF explainer's own example - the original design used non-resident, mirroring the native FIDO2 second factor's own choice, but that turned out to be wrong in practice: Windows Hello's OS-level WebAuthn API (`webauthn.dll`) hard-requires a resident credential before it will expose `hmac-secret`/PRF at all, so non-resident silently broke PRF on the most common desktop platform authenticator - found via a real manual test, not research, since the failure mode (registration succeeds, but `prf.enabled` comes back false) looks identical to "this authenticator just doesn't support PRF"; (1b) authenticator support genuinely varies beyond that fix too - Bitwarden's browser extension does not currently return a PRF output when acting as a passkey provider for third-party sites (confirmed via an open upstream GitHub discussion; other users hit the identical symptom with other E2EE apps), so a real user's first attempt may reasonably be with an unsupported authenticator and the error needs to stay clear about that; (2) no CLI or native-desktop equivalent, and none is planned for this specific mechanism - native already has USB-HID FIDO2, and there's no browser in a terminal or on the desktop build to reach `navigator.credentials` from; (3) the three-flag v10 header validation was generalized from a pairwise `!=` check to `count_ones() <= 1` so it scales to any number of mutually-exclusive second factors, not just two; (4) no automated end-to-end browser-passkey test coverage exists (no virtual-authenticator harness in this repo, unlike e.g. a headless-browser WebDriver setup) - covered by Rust-side plumbing tests (flag validation, secret derivation, roundtrip against a fake PRF output) plus a manual smoke test, similar in spirit to how the tlock item's live-relay tests are `#[ignore]`d rather than faked.

**Post-ship decision, same day**: after the resident-credential fix, a real end-to-end manual test across three browsers on the same Windows machine (no hardware security key available) found PRF support too inconsistent to leave enabled: Windows Hello via Edge registers and reports `prf.enabled: true`, but the follow-up `get()`/eval call returns no output; Firefox fails at registration itself with a generic "operation failed for an unknown transient reason" error, unresolved even after paring `pubKeyCredParams` down to a single ES256 entry (removing RS256, a plausible but unconfirmed TPM-resident-credential culprit); Bitwarden (item 1b above) doesn't implement PRF for third-party sites at all. Rather than ship a feature that reliably fails for real users today, the "Passkey" second-factor button is disabled in both tabs (`ui.add_enabled_ui(false, ...)` with an explanatory hover tooltip) while the underlying implementation - format, library, and GUI wiring - stays in place, fully tested, and ready to re-enable with a one-line change once browser/OS support for PRF evaluation (not just the enabled-flag check) actually stabilizes. Revisit by periodically retrying the same three-browser manual test.

Two bugs found only by actually loading the compiled wasm output in a real browser (neither caught by `cargo build --target wasm32-unknown-unknown`, `trunk build`, or clippy, since both are runtime panics, not compile errors): (a) **pre-existing**, unrelated to this feature - `pqfile-gui::types::current_unix_secs` used raw `std::time::SystemTime::now()`, which panics on `wasm32-unknown-unknown` ("time not implemented on this platform"); called from `PqfileApp::default()` to seed the keygen expiry date picker, so the web GUI panicked during app construction, before the first frame ever rendered, and the loading screen never cleared - true of every prior GUI feature, not just this one, just never previously caught in a real browser. Fixed by switching to `js_sys::Date::now()` on wasm32 (native unchanged); the clipboard tab's auto-clear timer had the identical `std::time::Instant::now()` gap and was fixed the same way, using wall-clock seconds instead of a monotonic clock it didn't need. (b) WebAuthn's relying-party ID must be a valid domain string - testing over `http://127.0.0.1:PORT/` fails passkey registration with "invalid domain" even though that origin is a valid *secure context*; `http://localhost:PORT/` (which `trunk serve` also listens on) is required instead. Documented in `README.md`'s WebAuthn section.

### 21. Forward-error-correction for cold-storage resilience *(shipped 2026-07-22)*

Shipped as `pqfile::fec` (`fec` cargo feature) plus `--fec` on `encrypt`/`decrypt`/`check`. Uses classical Reed-Solomon BCH error correction over the **ciphertext** bytes (a separate `<output>.pqf.fec` sidecar, not interleaved - applies uniformly to every format version with no wire-format change), correcting up to 4 corrupted bytes per 128-byte block. `FecRepairReader` repairs transparently before decrypt/check sees the data; an uncorrectable block passes through unchanged, so a genuine tamper failure still fails exactly as without FEC - purely additive to authentication. `pqfile-gui`'s `fec` feature is on by default with native auto-detection of a sibling `.fec` file. Full detail in `docs/CHANGELOG.md`.

### 22. Content-defined chunking and dedup for PQFA archives *(added 2026-07-14)*

The `.pqfa` archive format packs files as opaque entries with no cross-entry structure, so re-archiving a directory where only a few files changed re-encrypts and re-stores everything. restic, Borg, and Kopia all chunk file content with a rolling hash (Buzhash/FastCDC-style) and store each unique chunk exactly once, referenced by its hash - repeated or overlapping backups shrink dramatically. Natural pairing with the already-planned PQFA v2 per-entry AEAD (see the v5.0 section): a chunk index keyed by BLAKE3(chunk) (per the standing BLAKE3-for-non-format-hashing guideline above, since chunk identity never touches the wire format's authentication) would let PQFA store each unique chunk once and support incremental re-encryption of only the changed chunks on repeated archive operations. Scope carefully before starting: chunk-level dedup is a bigger design surface than PQFA v2's per-entry AEAD alone, and interacts with it (chunk boundaries vs. entry boundaries are different concerns).

### 23. Steganographic key backup *(shipped 2026-07-16)*

Shipped as `pqfile::stego` (`bury`/`exhume`/`capacity`), CLI `bury`/`exhume`, and a Keys-tab GUI panel (desktop + web, native and web both on by default), behind an off-by-default `stego` Cargo feature. Keyed LSB embedding in a PNG/JPEG cover's RGB8 pixel bytes (output always re-encoded as lossless PNG); a passphrase-derived BLAKE3-XOF keystream means a wrong passphrase is indistinguishable from an absent payload (`StegoPayloadNotFound` collapses every failure mode deliberately). Known limitation, not yet addressed: bits are placed sequentially rather than at a passphrase-seeded pseudo-random offset, so LSB statistics can flag that *something* is embedded even though the passphrase prevents confirming or recovering *what*. Full detail (including the pre-release keyed-detection security revision) in `docs/CHANGELOG.md`.

### 24. NIST additional PQC signatures ("on-ramp": HAWK, SQIsign, MAYO, etc.) - watch only *(added 2026-07-14)*

NIST's post-ML-DSA/SLH-DSA/FN-DSA signature track (distinct from all three already tracked above) advanced 9 candidates - FAEST, HAWK, MAYO, MQOM, QR-UOV, SDitH, SNOVA, SQIsign, and UOV - to Round 3 on 2026-05-14 (NIST IR 8610); CROSS was eliminated after a parameter-breaking attack forced an uncompetitive revision. HAWK is the sole lattice-based survivor at a notably smaller signature size than ML-DSA (555 bytes at category 1); SQIsign posted large signing/verification speedups since Round 1. Realistic standardization is 2028-2030 given Round 3's ~2-year evaluation window and another NIST conference planned for H1 2027. Nothing actionable today - recorded here so the on-ramp isn't silently absent from this document - but worth a check-in alongside the FN-DSA and OpenPGP-PQC items each time those are revisited.

### 25. Transparent cloud-sync/backup overlay (rclone-crypt / gocryptfs style) *(added 2026-07-20)*

Both `rclone` ([issue #3567](https://github.com/rclone/rclone/issues/3567)) and `restic` ([issue #4738](https://github.com/restic/restic/issues/4738)) have long-open, unresolved feature requests for post-quantum encryption, and the broader 2026 backup-tool ecosystem (Restic, BorgBackup, Kopia, Duplicati) has confirmed no PQC plans - AES-256-GCM/ChaCha20-Poly1305 remain the norm there with no near-term movement. Rather than waiting on any of those projects, or reimplementing sync/dedup logic pqfile has no reason to own, a transparent overlay mirrors a plaintext directory tree onto a directory of `.pqf` files one-to-one (à la gocryptfs or rclone's own `crypt` remote), mounted via FUSE on Linux/macOS and Dokan or WinFsp on Windows. Any file-based sync or backup tool - `rclone sync`, Restic, a cloud-drive client, plain `rsync` - then only ever sees opaque per-file ciphertext, without any of those tools needing to add PQC themselves. This is a distribution channel none of the other 24 items target: it puts pqfile in the workflow of every existing backup/sync user rather than requiring them to adopt a new tool. Natural pairing with the already-planned PQFA v2 per-entry AEAD and content-defined-chunking/dedup work (items in the v5.0 section and item 22) if incremental re-encryption of only changed files becomes a goal, though a first cut needs neither - one plaintext file to one `.pqf` file, re-encrypted whole on change, is already a complete and useful v1. Filename confidentiality is an open design question worth deciding up front: mirroring plaintext filenames leaks directory structure and names to the storage backend, while encrypting them (as gocryptfs does) requires a deterministic, collision-resistant filename scheme that hasn't been designed here yet.

### 26. Named recipient groups in config.toml *(shipped 2026-07-23)*

Shipped as a `[groups]` table in `config.toml` (`team-security = ["pqf1...", "pqf1...", "path/to/pubkey.pem"]`); `encrypt -r team-security` expands to every member. One level of expansion only (no nested-group resolution), no wire-format change. Full detail in `docs/CHANGELOG.md`.

### 27. Batch/recursive rekey and key rotation *(shipped 2026-07-23)*

Shipped as `pqfile rotate --old-key <PRIVKEY> --new-key <PUBKEY> --recursive <DIR>`: walks the tree, calls the existing zero-copy `rekey::rekey_stream` on every `.pqf` file found, rewrites each in place, and reports a succeeded/failed/skipped-non-pqf summary. No new crypto, no format change; inherits `rekey`'s own v3/v5-only, default-chunk-size-only restriction, so an unsupported `.pqf` file is reported as a per-file failure rather than skipped or aborting the batch. Full detail in `docs/CHANGELOG.md`.

### 28. Git clean/smudge filter integration *(added 2026-07-23)*

A `pqfile git-filter clean`/`pqfile git-filter smudge` pair (or a small standalone `pqfile-git` binary) implementing git's [clean/smudge filter protocol](https://git-scm.com/docs/gitattributes#_filter), so files matched by a `.gitattributes` pattern are transparently encrypted on `git add`/commit and decrypted on checkout - the git-crypt/git-secret/transcrypt niche, but post-quantum. This targets a workflow none of the other distribution-channel items (age plugin, WKD below, cloud-sync overlay) reach: encrypting specific paths *inside* an otherwise-plaintext git repo (secrets files, `.env`s, credentials committed by necessity) rather than encrypting whole files or directories outside git's view. `smudge` needs a private key available at checkout time (an SSH-agent-style long-lived unlock, or the OS-credential-store hardware-key path already shipped, would avoid a passphrase prompt on every `git checkout`); `clean` only needs a public key, so it can run in CI without any secret material present. Config-file recipient defaults (shipped 2026-07-01) cover most of the plumbing already. Git stores the clean filter's *output* in the object database and diffs against it, so `git diff`/`git log -p` on an encrypted path shows ciphertext by design, same tradeoff git-crypt itself makes.

### 29. Recipient key discovery (Web Key Directory style) *(added 2026-07-23)*

An HTTPS well-known-path convention (`https://<domain>/.well-known/pqfile/<local-part>`, mirroring OpenPGP's [WKD](https://wiki.gnupg.org/WKD)) so `encrypt -r alice@example.com` can resolve and fetch Alice's `pqf1…` recipient string or certificate automatically, instead of requiring an out-of-band key exchange first. Pairs naturally with the cert/revocation-list infrastructure already shipped (2026-07-13/15): a domain could publish a certificate rather than a bare key, giving the fetched key an expiry window and revocation-list coverage for free via the existing `--revocations` flag. Must be opt-in and off by default, matching the `tlock`/`update-check` precedent for the only network-capable code paths in the workspace today - a plain build stays network-free. Lower priority than the age-plugin item (2) it sits near in spirit, since WKD requires the recipient's *domain operator* to publish something, not just the recipient; the age ecosystem interop item has no equivalent publishing dependency.

### 30. Opt-in passphrase breach check (k-anonymity) *(added 2026-07-23)*

`encrypt --passphrase` (v10) already runs `passphrase_strength` locally and warns on weak passphrases, but strength scoring cannot catch a passphrase that is *structurally* strong yet already sitting in a known breach corpus. An opt-in check against a Have I Been Pwned-style range API - sending only the first 5 hex characters of a SHA-1 hash and matching the full hash client-side against the returned suffix list, so the passphrase itself never leaves the machine - would close that gap the same way `check-update` and `tlock` already carve out explicit, off-by-default network exceptions to the "all cryptographic operations run locally" framing (`docs/SECURITY.md` already narrates both existing exceptions by name; this would be a third, worth adding to that same paragraph rather than treating the invariant as absolute). Purely advisory - a hit should warn, never block, since the breach corpus has no way to distinguish "this passphrase is compromised" from "this passphrase coincidentally collides in a large public list."

### 31. Symbolic protocol verification (ProVerif / Tamarin model) *(added 2026-07-23)*

Every verification effort so far targets either a single primitive (the `kem-libcrux` formally-verified backend, item in the completed list) or an implementation detail (Wycheproof/ACVP known-answer tests, the `ct_*` dudect constant-time harnesses). None of them model the *protocol composition* - how multi-recipient key-wrapping, `signcrypt`, and sealed sender's static-static X25519 deniability property (§6.6, shipped 2026-07-15) actually compose across format versions. A symbolic model in ProVerif or Tamarin of, at minimum, the sealed-sender deniability claim and the signcrypt authenticated-encryption composition would catch a logic-level flaw (key confusion across formats, an unintended oracle from mixing multi-recipient slots with a certificate-resolved recipient) that no amount of primitive-level or implementation-level testing is positioned to find - the kind of gap that historically surfaces as an academic paper years after shipping, not a fuzzer crash. This is a modeling effort, not a code change to `pqfile` itself, so it can proceed independently of any release cycle; ranked last among the newly-added items because it requires cryptographic-protocol-modeling expertise this project hasn't needed before, not because the payoff is small.

### 32. Secure peer-to-peer transfer mode *(added 2026-07-23)*

A `pqfile send`/`pqfile recv` pairing (in the spirit of Magic Wormhole: a short human-readable code, a PAKE-derived session, direct or relay-assisted transfer) so two parties can move a file without either already holding the other's public key - every existing pqfile workflow assumes the recipient's key is already known. This is the largest-scope item added in this pass and sits uneasily against the standing "all cryptographic operations run locally; no data is sent to a server" security invariant (`docs/ROADMAP.md` "Security invariants" section) - a relay-assisted fallback for NAT traversal is an explicit, opt-in exception to add there, not a silent one, if this is ever scoped further. The PAKE step itself is also an open research question in the same way proxy re-encryption (item 19) is: mature *post-quantum* PAKE constructions are less settled than PQ KEMs/signatures, so this would likely start from a classical PAKE (CPace, SPAKE2) for session bootstrapping with the actual file payload still encrypted via pqfile's existing PQ primitives - the transfer channel's authentication would be classical even though the file encryption itself stays post-quantum, a caveat worth stating up front rather than discovering mid-implementation. Watch-only for now; revisit once a specific PAKE choice can be justified the way X-Wing was justified for the hybrid KEM.

### Constant-time test harness extension *(complete)*

`pqfile/examples/ct_shamir.rs` (Shamir GF(256) reconstruction), `ct_decrypt.rs` (decryption error path: tamper-position timing), and `ct_passphrase.rs` (wrong-passphrase rejection: unrelated vs near-miss guess) are standalone dudect-style Welch t-test binaries. All three require a quiet machine and ≥100 000 samples for a meaningful verdict; they are deliberately not run in CI.

---



## Evaluated and not planned

Considered during the 2026-07-08 ecosystem review and deliberately left off the roadmap, recorded so they are not re-litigated from scratch:

- **Ascon-AEAD128 (NIST SP 800-232)**: standardized in 2025 for constrained devices, but it is slower than ChaCha20-Poly1305 on every platform pqfile targets (desktop, server, WASM), and the Rust crate is unaudited. No fit unless an embedded target appears.
- **Miri /** `cargo-careful` **CI jobs**: `#![deny(unsafe_code)]` at the crate root leaves almost no first-party unsafe surface (one sanctioned mmap call), and dependency unsafe is better covered by the existing fuzzing plus `cargo vet`. Poor signal per CI minute.
- **io_uring async I/O (**`tokio-uring` **/** `compio`**)**: encryption throughput is CPU-bound on the AEAD and KDF paths, not syscall-bound; the Linux-only complexity is not justified by the profile.
- `memsecurity` **/ ASCON-encrypted in-RAM secrets**: encrypting secrets in memory with a key that lives in the same address space adds obfuscation, not a security boundary. The `memsec` mlock approach (shipped 2026-07-08 as `pqfile/src/secret.rs`) addresses the actual threat, which is swap and crash dumps.

Considered during the 2026-07-13 review and deliberately left off the roadmap:

- **Physical side-channel (power/EM) countermeasures for ML-KEM/ML-DSA**: 2026 research (e.g. eprint 2026/056, the PROACT2025 survey) demonstrated real power/EM attacks against unprotected ML-KEM and ML-DSA implementations, including full ML-KEM key recovery in minutes. These attacks require an adversary with physical instrumented access to the running device (a probe on the chip or its power rail) - a threat model that smartcards and embedded secure elements must defend against, but that a CLI/GUI/WASM application running on a general-purpose desktop or server OS does not claim to. Masking countermeasures at that level are out of scope here. This is a distinct concern from the *local/remote timing* side-channels pqfile already defends against via the `ct_`* dudect harnesses (item stays in scope); it is specifically about physical instrumentation, which is a different attacker.
- **FIPS 140-3 cryptographic module validation**: pqfile builds on RustCrypto's pure-software `ml-kem`/`ml-dsa`/AEAD implementations (plus the optional `libcrux-ml-kem` backend), none of which is an independently validated FIPS 140-3 module. Formal CMVP validation is a multi-year, six-figure-plus process scoped to a specific module boundary and build, and would need to be redone on every dependency bump - not a fit for an actively-developed OSS crate. Noted for awareness given Executive Order 14412 (June 2026) sets a 2031 deadline for US federal high-value systems to move to post-quantum signatures, but pqfile is not pursuing federal-procurement compliance as a goal.

---



## Security invariants

The following properties are invariants, not roadmap items. Any proposal that weakens them requires explicit justification and a major version bump:

- All cryptographic operations run locally. No data is sent to a server.
- The entire `.pqf` file (header + payload) is authenticated before any plaintext is returned.
- Secret material is zeroized from memory on drop.
- Each encryption produces a fresh KEM ciphertext and a fresh random nonce.
- In hybrid mode, each encryption also produces a fresh ephemeral X25519 scalar.

